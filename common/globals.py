no_gui = True        # Flag to disable GUI features

symbol_table = None  # Storage for variables and functions and their values
tokens = None  # Storage for tokens (list of tuples with token, tag, and line number)
